<?			include_once "local/monParams-FTS.php";			                        $command=$_GET["command"];            $curr_server=getenv("SERVER_NAME");                        echo "<title>Tail of $command Log</title>";            echo "<h2>Tail of $command Log  ($PAserver)</h2> ";            $tail = "";		    if($PAserver!=$curr_server){             	$chack_tag_cmd="ssh -2 prodagent@pccms28 'ssh pccms27 'tail -n500 $prodarea/$command/ComponentLog''";				exec($chack_tag_cmd, $retval);				foreach($retval as $value){					//echo $value."<br>";				}				$retval = array_slice ($retval, 12);				$tail = implode("\n", $retval);			}            else {           $tail = `tail -n500 $prodarea/$command/ComponentLog`;}            $tail=nl2br($tail);			echo $tail;?>			