<?xml version="1.0" ?>
<!-- This is the configuration file for UpdateGlobalDatabase.py, an
     standalone application that is intended to be run outside the ProdAgent
     environment. The description of the most important parameters follow:

     PollInterval: interval used to update the global database (seconds).
     Host: hostname where the MySQL server is running.
     User: user name with database creation abilities.
     Password: its password.
     SocketFileLocation: location of the socket file for database access.
     PortNr: port number for database (takes precedence over socket).

     For each ProdAgent instance, there must be a name associated, which
     will be used as the name of the dump file (with added extension .sql),
     and also name of the target database created on the server. It should
     follow then filename and database name conventions.

    The import command can be multi-line, and should list all commands needed
    to get the file provided by the Monitoring component. The expected name
    of the file should be 'prodagentname.sql'. In case of errors during
    import, the messages are displayed in the log file.

    Fields are filled with some examples, which of course should be updated.
 -->
<MonitoringConfig>
	<Parameter Name="PollInterval" Value="3600"/>
	<Parameter Name="Host" Value="localhost"/>
	<Parameter Name="User" Value="ProdAgentUser"/>
	<Parameter Name="Password" Value="ProdAgentPassword"/>
	<Parameter Name="SocketFileLocation" Value="/localdisk/kavka/PA.head/mysqldata/mysql.sock"/>
	<Parameter Name="PortNr" Value=""/>
	<ProdAgentInstance Name="CarlosPA">
		<ImportCommand>
			cp ../ProdAgentStatus.sql CarlosPA.sql
		</ImportCommand>
	</ProdAgentInstance>
        <ProdAgentInstance Name="AlePA">
                <ImportCommand>
                        wget http://www.somehost.it/~ale/ProdAgentStatus.sql
			mv ProdAgentStatus.sql AlePA.sql
                </ImportCommand>
        </ProdAgentInstance>
</MonitoringConfig>

